# brand_rules.yaml — per-brand invoicing rules. Rules are config, not code:
# new brands get added here, no code change.
#
# TODO with Huong/Dashaini: complete list of brands needing separate invoices
# (Keoh is the known one — any others?).

defaults:
  invoice_grouping: combined

brands:
  keoh:
    invoice_grouping: separate # Keoh gets its own invoice split
