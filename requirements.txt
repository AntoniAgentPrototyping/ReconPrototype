# Single source of truth for dependencies is pyproject.toml.
# This file is kept so existing docs and habits ("pip install -r
# requirements.txt") keep working; it deliberately pins nothing itself.
#
#   pip install -e .          # runtime
#   pip install -e ".[dev]"   # runtime + pytest
#
# Note: pandas is bounded to <3 and Python to <3.14 in pyproject.toml. Both
# are controls, not oversights — see the comments there before lifting either.
-e .
